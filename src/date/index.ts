// 格式化时间
export { dateFormater } from './_dateFormater'

// 当前时间距离传入的时候过去了多久
export { formatPassTime } from './_formatPassTime'

// 距离目标时间还有多久
export { formatRemainTime } from './_formatRemainTime'

// 计算两日期之间相差的天数
export { dayDiff } from './_dayDiff'

// 计算两个日期之间的月数
export { monthDiff } from './_monthDiff'

// 比较两个日期
export { compareDate } from './_compareDate'

// 将秒转换为hh:mm:ss格式
export { formatSeconds } from './_formatSeconds'

// 获取日期属于哪个季度
export { getQuarter } from './_getQuarter'

// 获取传入的日期当月的最后一个日期
export { getLastDate } from './_getLastDate'

// 获取传入的日期当月的第一个日期
export { getFirstDate } from './_getFirstDate'

// 获取当前时间戳（秒）
export { getSecond } from './_getSecond' 